[profile.default]
src = 'contracts'
out = 'out'
libs = ['node_modules', 'lib']
test = 'test'
cache_path  = 'cache_forge'
verbosity=2

[rpc_endpoints]
polygon = "${POLYGON_RPC_URL}"

[polygonscan]
polygon= { key = "${POLYGON_API_KEY}" }